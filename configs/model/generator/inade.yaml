_target_ : models.generator.INADEGenerator
num_filters_last_layer: 64
num_up_layers: 7
height: ${dataset.height}
width: ${dataset.width}
num_labels: ${dataset.num_labels}
noise_dim: 64
kernel_size: 3
num_output_channels: ${dataset.num_channels}
apply_spectral_norm: True